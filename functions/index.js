// Cloud Functions removed — all user management handled client-side.
// Legacy functions (orders, QC, PO, cash, expenses, worker payments) deleted.
// App runs on Spark (free) plan: Firestore + Auth only, zero Cloud Functions.
