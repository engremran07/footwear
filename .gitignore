# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
debug.log
firebase-debug.log
firestore-debug.log
ui-debug.log
database-debug.log
pubsub-debug.log

# Device / debug log captures
*_logs.txt
*_logs_*.txt
*_errors.txt
app_logs.txt
fresh_logs.txt
fresh_errors.txt
device_logs_*.txt

# Firebase
.firebase/

# Node.js (functions)
functions/node_modules/
functions/lib/
functions/package-lock.json

# Flutter — build outputs
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
app/pubspec.lock

# Android Gradle
app/android/.gradle/
app/android/local.properties

# iOS (not used)
app/ios/Flutter/Flutter.framework
app/ios/Flutter/Flutter.podspec
app/ios/Pods/
app/ios/GoogleService-Info.plist

# Web (no web version)
app/web/build/

# Firebase generated config (contains project IDs — keep tracked)
# app/lib/firebase_options.dart  <- intentionally tracked

# Secrets / credentials
app/android/app/google-services.json
google-services.json
GoogleService-Info.plist
.env
.env.local
*.key
*.jks
*.keystore
app/android/key.properties

# Auth export (contains hashed credentials)
auth-users.json

# Release APKs (binary artifacts, distribute separately)
releases/
*.apk
*.aab

# Dev utility scripts (local only)
check_locale.ps1
check_locale.py

# IDE
.idea/
.vscode/settings.json
*.iml
